// ////////////////////////////////////////////////////////////////////////////
// Common wrapper for logging.
'use strict';
const bunyan = require('bunyan');

exports = module.exports = {
    createLogger: function (name, logLevel) {
        const logger = bunyan.createLogger({ name: name, level: logLevel });
        return logger;
    },
};
